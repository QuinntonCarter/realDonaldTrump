export default function MediaOverlay({ ...props }) {
  // modal?
  //   contains <Media/> component to display media dynamically
  // contains tweaked <Tweet/> component for media overlay mode
  return <></>;
}
